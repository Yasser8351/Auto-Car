import 'package:flutter/material.dart';

class ContectUsScreen extends StatelessWidget {
  const ContectUsScreen({Key? key}) : super(key: key);
  static const routeName = "ContectUsScreen";

  @override
  Widget build(BuildContext context) {
    //ContectUsScreen
    return Scaffold();
  }
}
